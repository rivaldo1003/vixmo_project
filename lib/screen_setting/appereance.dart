import 'package:flutter/material.dart';

class Appereance extends StatelessWidget {
  const Appereance({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Halaman Appereance'),
      ),
    );
  }
}
