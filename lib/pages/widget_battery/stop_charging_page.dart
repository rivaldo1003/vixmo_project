import 'package:flutter/material.dart';

class StopCharging extends StatelessWidget {
  const StopCharging({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Stop Charging'),
      ),
    );
  }
}
